The uploaded zip (matches-page-segment.zip) contained exactly one file:
matches-page-segment.html. There were NO image, font, CSS or JS assets inside.

- Company logos load at runtime from https://logo.clearbit.com/<domain> (external).
- All icons are text glyphs; the JobBot diamond is an inline SVG.
- The typeface is the system font stack (no webfonts in this design).

The original file is preserved in ../reference/original-matches-page-segment.html.
